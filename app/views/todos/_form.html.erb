<%= simple_form_for(todo, html: { class: 'form-horizontal' }) do |f| %>
  <%= f.input :description, as: :text %>
  <%= f.input_field :due_date, as: :date, start_year: Date.today.year %>
  <%= f.button :submit %>
<% end %>

